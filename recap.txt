CPP_00
	ex00
		decouverte bibliotheque <iostream>
		std::cout  << "..." << std::endl
		str.length()
		std::toupper() et syntaxe cast 

	ex01
		std::getline(std::cin, line);
		creation des classes(private/public)
		std::setw(10) -> formater largeur tableau
		str.substr(0, 9) + '.' -> reduire nombre caracteres pour affichage
		encapsulation (attributs en private accessibles depuis methodes get et set publiques)
		tableau statique (contacts[8]) donc pas d'alloc dynamique

	ex02
		bibliotheque <ctime> recuperation date/heure :
				time_t now = time(NULL);
				tm *ltm = gmtime(&now);
				std::cout << "[" << 1900 + ltm->tm_year << ... << "] ";
		variables statiques a declarer dans le .cpp

CPP_01
	ex00
		utilisation 
			constructeur : Permet d'initialiser un objet avec valeur(s) spécifique(s) dès sa création
			destructeur : Detruit un objet a la fin de l'execution du programme
		allocation  automatique : Zombie z("Bob")
		allocation dynamique : Zombie* z = newZombie("Bob")
			> cree un objet durable accessible via son adresse pointee par z.
			> le detruire manuellement (delete z) lorsqu'on en a plus besoin
		si un zombie est cree dans une fonction de facon automatique il est detruit lorsqu'on sortira de cette fonction

	ex01
		allocation dynamique de tableau d'objet : horde = new Zombie[N];
			> alloue N zombies avec le constructeur par defaut (aucune info sur les zombies renseignees(pas de nom,...))
			> le detruire manuellement (delete[] Zombie) lorsqu'on en a plus besoin

	ex02
		pointeur : adresse d’une variable et peut être redirigé vers une autre ou être nul (nullptr),
					mais nécessite un déréférencement (*ptr) pour accéder à la valeur.
		reference : alias constant vers une variable existante.
					Elle ne peut pas être changée pour viser autre chose, ni être null.
					(permet de créer un deuxième nom pour une même variable 
					si la valeur de cette variable est modifiée via l’un des deux noms,
					le changement est visible par l’autre également.)

	ex03
		implementer des classes avec des attributs  passes par references ou pointeurs
		Si l'attribut est passe par reference, sa valeur doit etre initialisee lors de la construction ( une ref ne peut pas etre nulle)
		Si l'attribut est passe par pointeur, sa valeur n'a pas besoin d'etre initialisee lors de la construction et peut donc etre nulle

	ex04
		bibliotheque <fstream> pour lecture/ecriture d'objets
		std::ifstream inputFile(argv[1]); -> Création d’un objet inputFile permettant de lire le fichier indiqué par argv[1]
			-> xx.is_open() : vérifie si le fichier a bien été ouvert avec succès. Ne vérifie pas s’il est en bon état ou lisible, juste s’il est ouvert
			-> xx.good() : vérifie si le flux de fichier est dans un état valide.
		std::ofstream outputFile(file_replace.c_str()); (file_replace = nom de l'objet a creer)
			-> crée un objet outputFile de type std::ofstream (fichier de sortie) ouvert en écriture dans le fichier dont le nom est contenu dans la variable file_replace.
			-> file_replace.c_str() caste std::string en const char * car ofstream prend en parametre ce dernier type
		fermer manuellement les objets en lecture/ecriture avant de terminer le programme (inputFile.close() et outputFile.close())
		verifier si l'objet a ouvrir n'est pas un dossier avec bibibliotheque <sys/stat.h>:
			stat(path, &path_stat) remplit la structure path_stat avec des infos sur le fichier ou dossier pointé par path.
			S_ISDIR(path_stat.st_mode) vérifie si l’élément désigné est un répertoire (dossier).

	ex05
		Création d’un tableau de pointeurs sur méthodes membres :
			-> syntaxe → [type de retour] (NomClasse::*)([arguments])
			-> exemple → void (Harl::*functions[])()
		Paramètres passés : les adresses des méthodes membres à appeler, sous la forme :
			-> { &NomClasse::methode1, &NomClasse::methode2, ... }

	ex06
		outil switch case default
			-> switch (int ou char) - case [1,2,3,...,a,b,c,...]: Permet de tester une variable entière (souvent int ou char) contre plusieurs valeurs précises.
			-> break : essentiel pour éviter l’effet de “chute” (fallthrough). Sans break, le programme exécute aussi tous les case suivants, même si leur condition ne correspond pas.
			-> default : Bloc exécuté si aucun case ne correspond

CPP_02
	ex00
		utilisation Forme canonique
			-> constructeur par defaut : Initialise un objet sans argument, souvent avec une valeur neutre ou par défaut.
			-> constructeur par copie : Crée un nouvel objet en copiant proprement un autre, champ par champ.
			-> Assignation par operateur de copie (=) : Permet de copier le contenu d’un objet vers un autre déjà existant, tout en évitant les fuites mémoire ou effets indésirables.
			-> destructeur : Libère proprement les ressources (s’il y en a) à la destruction de l’objet.

	ex01
		manipulation de bit pour stockage/transformation des float - précision 1/256 pour float(32 bits)
			-> int : decalage de 8 bits bruts ( 8 zeros vers la gauche) (_rawBits = entier << 8)
			-> float : on decale les bits de 8 positions et on arrondi (par exemple partie entiere : 110100101 - partie decimale :10010011 apres decalage 11010010110010011) (_rawBits = roundf(flotant * 256));
		assignation d'un attribut constant dans le .cpp

	ex02
		surcharges d'operateurs

	ex03
		Exercice d'application des notions du module.
		Objectif de l'exercice : trouver un point dans un triangle.

CPP_03
	ex00
		Mise en situation des tous les exercices précédents
			forme canonique
			methodes
			setters
			getters

	ex01
		🧬 Héritage
			class ScavTrap : public ClapTrap
			ScavTrap hérite des attributs et méthodes publiques/protégées de ClapTrap.
				•	Lors de la construction, ClapTrap est construit en premier, puis ScavTrap.
				•	Pour garantir une destruction correcte via un pointeur de base, le destructeur de ClapTrap doit être virtual.
		🧠 Polymorphisme dynamique
			Une méthode marquée virtual permet d’être surchargée dans les classes enfants,
			et le programme appelle automatiquement la version la plus dérivée à l’exécution.
				•	La méthode peut être déclarée virtual dans le parent, même si elle n’est pas encore définie.
				•	Cela permet de manipuler des objets enfants via des pointeurs ou références de classe de base tout en conservant leur comportement propre.

	ex02
		Idem ex01, on cree une seconde classe FragTrap qui hérite de ClapTrap mais avec des fonctionnalités différentes de ScavTrap

	ex03
		Héritage multiple
			class DiamondTrap : public ScavTrap, public FragTrap
			DiamondTrap hérite deux fois indirectement de ClapTrap (via ScavTrap et FragTrap), ce qui crée une ambiguïté sur les membres hérités.
			Cela oblige à spécifier le chemin d’accès pour éviter les collisions :
				ScavTrap::attack(target);

CPP_04
	ex00
		héritage et polymorphisme dynamique pour que la fonction makesound soit adaptée a la bonne instance d'animal.
		effet de l'utilisation de virtual avec la classe wronganimal qui n'utilise pas volontairement void
			resultat, la fonction de la classe animal et directement utilisé donc les déclarations de cette fonction dans les enfants n'est pas appelé et utilisée.

	ex01
		utilisation d'une classe (brain) en attributs d'une autre classe
		initialisation et implémentation de celle-ci 

	ex02
		Notion de classe abstraite
		L’exercice montre comment intégrer une classe (Brain) comme attribut non-pointeur dans une autre (Dog, Cat)
		et comment gérer proprement sa construction, destruction et copie, notamment via la forme canonique.
		Brain est composée dans chaque Dog ou Cat :
			appeler son constructeur dans le constructeur du Dog ou Cat
			gérer sa copie profonde dans le constructeur de copie et l’opérateur d’affectation
			gérer la mémoire (delete)
			bien respecter l’ordre de construction/destruction

	ex03
		Notion de classes purements abstraites : l'interface
			Tous les éléments sont des méthodes virtuelles pures déclarées en virtual ... = 0;
			Seules ses classes dérivées peuvent être utilisées et elles doivent obligatoirement implémenter toutes ses méthodes.

CPP_05
	ex00
		Apprendre à créer une classe avec des valeurs contraintes et à gérer les erreurs avec des exceptions personnalisées.
		Permet d’afficher une erreur sans utiliser de return ou de booléens, en gérant proprement l’exception 
		Creation d'une classe dans un classe (.hpp):
			class GradeTooHighException : public std::exception {
			public:
				const char* what() const throw();};

		Declaratin de la méthodev(pièces spécifié toutes les classes  dans lesquelles elle se trouve) :
			const char* Bureaucrat::GradeTooHighException::what() const throw()
			{
				return ("Grade is too high!");
			}

		Exemple d'appel à une exception :
			if (_grade < 1)
				throw GradeTooHighException();

		Exemple de test à effectuer. On teste la fonction si ça fonctionne ça sort sinon ça applique les particularités du catch:
			try {
			Bureaucrat b("Jean", 0); // Exception ici
			}
			catch (const std::exception& e) {
				std::cerr << e.what() << std::endl;
			}

	ex01
		•	Une classe peut agir sur une autre
			→ Ici : Bureaucrat agit sur un Form via la méthode beSigned(Bureaucrat&).
		•	Gestion d’erreur à l’intérieur d’une méthode via throw
			→ Si le grade du bureaucrate est trop bas, beSigned() lance une exception.
			→ C’est ensuite au code appelant de gérer l’exception avec try / catch.
		•	Structure multi-classe :
			•	Chaque .cpp n’a besoin que de l’#include du .hpp des classes qu’il utilise
			•	Bien gérer les inclusions croisées (ex : #include "Bureaucrat.hpp" dans form.cpp et "class Bureaucrat;" dans Form.hpp)
		•	Attributs constants (const)
			→ Doivent être initialisés dans le constructeur, jamais modifiés ensuite.

	ex02
		Création de formulaires héritants la classe form transformée en Aform car la méthode execute est abstraite pour que les méthodes enfants aient leurs propres exécution de cette méthode
		Vérification des conditions d'exécution du formulaire : grade suffisant + signé

	ex03
		Continuité de l'apprentissage des tableaux sur pointeurs de méthodes
			Évite l'usage de if/else à foison
		Attention, les méthodes doivent t'appartenir à la classe dans laquelle on utilise le tableau sur pointeur.
		Il faut donc créer les méthodes nécessaires pour exécuter des méthodes déclarés dans d'autres classes.

CPP_06
	ex00
		apprentissage des casts : static_cast<T>(d)
		précision des décimales dans les float et doubles : std::cout << std::fixed << std::setprecision(1);
		utilisation des fonctions std::stoi,stof,stdod pour convertir une string en int, float ou double
		définition de scalaire : 
			•	Un scalaire est une valeur simple, à une dimension, par opposition à un vecteur, une matrice, une structure, etc.
			•	En C++, on appelle types scalaires : char, int, float, double (mais aussi bool, nullptr_t, les pointeurs, etc.).
		Ici, le but est de convertir un scalaire d’un type à un autre (par exemple : "42" → char ' * ', int 42, float 42.0f, double 42.0).

	ex01
		